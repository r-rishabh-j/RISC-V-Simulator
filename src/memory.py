#!/usr/bin/python3
# code for memory module. We will most probably be using hashes for memory

memory=dict() # key as address, value as memory content. Memory byte addressable! Thus, every element stores a byte